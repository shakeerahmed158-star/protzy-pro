import { View, Text } from "react-native";

export default function BuyInventoryPage() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Buy Inventory Screen</Text>
    </View>
  );
}