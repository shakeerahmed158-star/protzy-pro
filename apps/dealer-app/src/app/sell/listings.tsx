import { View, Text } from "react-native";

export default function ListingsPage() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Sell Listings Screen</Text>
    </View>
  );
}