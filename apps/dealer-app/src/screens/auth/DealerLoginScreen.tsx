import { View, Text } from "react-native";
import { api } from "../../services/api";


export default function DealerLoginScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Dealer Login Screen</Text>
    </View>
  );
}